global:
  scrape_interval: 1s
  evaluation_interval: 1s

rule_files:
  - prometheus-rules.yml

scrape_configs:
  - job_name: capstone-loans-smoke
    static_configs:
      - targets:
          - 127.0.0.1:8001
